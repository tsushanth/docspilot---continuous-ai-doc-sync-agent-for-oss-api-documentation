# Sample API

A tiny fixture API used to test DocsPilot's drift detection.

## get_user

Call `get_user(user_id)` to fetch a user by their id. Returns a user dict,
or `None` if no such user exists.

## Installation

Run `pip install -e .` to install this package locally.
